NEXT_PUBLIC_API_BASE_URL=http://localhost:5051/api
# Same OAuth client ID as backend's GOOGLE_CLIENT_ID (safe to expose
# client-side — it's a public identifier, not a secret).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=xxxxxxxxxxxx.apps.googleusercontent.com
